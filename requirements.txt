# ── Core API + agent stack ────────────────────────────────────────────────────
langgraph>=0.2.0
langchain-anthropic>=0.2.0
fastapi>=0.112.0
uvicorn[standard]>=0.30.0
openai>=1.77.0
openai-agents>=0.18.0
python-dotenv==1.0.1

# ── Retrieval / RAG ───────────────────────────────────────────────────────────
sentence-transformers==2.6.1
faiss-cpu>=1.7.4

# ── Analytics tools ───────────────────────────────────────────────────────────
pandas>=2.1.0
numpy>=1.22,<2
scikit-learn>=1.3.0
xgboost>=2.0.0
torch>=2.0.0
# numba removed — Monte Carlo DCF uses vectorised NumPy (see tools/valuation.py)

# ── PDF export ────────────────────────────────────────────────────────────────
weasyprint>=62.0
jinja2>=3.1.0

# ── Cloud + DB ────────────────────────────────────────────────────────────────
boto3>=1.35.0
pymongo[srv]>=4.0.0
motor>=3.0.0
psycopg2-binary>=2.9.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ── Optional: Postgres LangGraph checkpointer (production) ───────────────────
# langgraph-checkpoint-postgres>=2.0.0

# ── Feast + Airflow + Prophet — install in the separate .venv-feast311 ────────
# These require Python <=3.12 and have conflicting numpy/grpc deps with the
# main stack. Use the dedicated venv:
#   python3.11 -m venv .venv-feast311
#   source .venv-feast311/bin/activate
#   pip install feast==0.38.0 prophet==1.1.5 "pyarrow>=17,<18" apache-airflow>=2.10.0
