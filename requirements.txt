langgraph>=0.2.0
langchain-anthropic>=0.2.0
fastapi>=0.115.0
uvicorn>=0.30.0

# For local dev, build_graph() defaults to an in-memory checkpointer
# (included above). For the postgres checkpointer referenced in agent.yaml
# once imported into watsonx Orchestrate, add:
# langgraph-checkpoint-postgres>=2.0.0
